spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Singapore
  profiles:
    active: dev
  application:
    name: usmsecondhand
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/usmsecondhand?serverTimezone=Asia/Shanghai
    username: root
    password: root
  #session ????
  session:
    timeout: 30m
    cookie:
      domain: localhost
      http-only: true
      # secure: true
    store-type: redis
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

#  mail:
#    host: smtp.gmail.com
#    port: 587
#    username: ${GMAIL_USERNAME:heitmanm804@gmail.com}
#    password: ${GMAIL_APP_PASSWORD:ohjy jirl xmqn dlvo}  # ⚠️ 应用专用密码，不是登录密码！
#    protocol: smtp
#    default-encoding: UTF-8
#    properties:
#      mail:
#        smtp:
#          auth: true
#          starttls:
#            enable: true
#          connectiontimeout: 10000
#          timeout: 10000
#          writetimeout: 10000
#        debug: true  # 开发时开启，可以看到详细的SMTP日志


server:
  port: 8081
  servlet:
    context-path: /api
    session:
      cookie:
        domain: localhost
file:
  # Windows 示例，注意斜杠
  upload-dir: E:/cmt322 secondhand/uploads/
  # 本地后端地址
  domain: http://localhost:8081/api

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: isDelete  # ??????????
      logic-delete-value: 1
      logic-not-delete-value: 0
